
// we should avoid using nested if statements as much as possible. 
let state = 'CA';
let dob = 30;
if(state == 'bd');{
    if(dob >= 18){
        console.log('You can drive.') // checke only last argument 
    };
};
// we can use the && operator to combine the conditions and use an if statements as follows:
let age = 30;
let countery = 'Bd';
if(age >=18 && countery == 'Bd'){
    console.log('You are sign up.')
};

